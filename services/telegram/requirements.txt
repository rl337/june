# Telegram Bot Service Dependencies
# Note: Most dependencies are already in june-base image

python-telegram-bot[webhooks]>=20.7
pydub>=0.25.1
httpx>=0.24.0
# psycopg2-binary>=2.9.0  # Removed - PostgreSQL not available

# Audio processing (for format conversion and quality analysis)
# Note: FFmpeg (which includes ffprobe) is installed in june-base Docker image.
# If OGG conversion fails with "ffprobe not found" errors, ensure june-base image is up to date.
numpy>=1.24.0
librosa>=0.10.1

# PDF generation for conversation export
reportlab>=4.0.0

# NATS for message queue
nats-py>=2.3.0

