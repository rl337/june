# Telegram Bot Service Dependencies
# Note: Most dependencies are already in june-base image

python-telegram-bot[webhooks]>=20.7
pydub>=0.25.1

# Audio processing (for format conversion)
# ffmpeg is installed in base image

